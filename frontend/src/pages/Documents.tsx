import { ArtifactExplorer } from '@/components/workspace/artifact-explorer'

/** Documents — the artifact explorer scoped to the "docs" workspace category. */
export function Documents() {
  return <ArtifactExplorer title="Documents" category="docs" />
}
