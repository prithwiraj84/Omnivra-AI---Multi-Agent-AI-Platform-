# Optional Document Studio render engine (cp-0025).
# Without these, document generation degrades gracefully to a markdown deliverable
# (stub=True). Install to get real downloadable PPTX / DOCX / PDF files:
#     pip install -r requirements-docs.txt
python-pptx==0.6.23   # .pptx (presentations)
python-docx==1.1.2    # .docx (Word documents)
fpdf2==2.7.9          # .pdf  (latin-1 core fonts)
